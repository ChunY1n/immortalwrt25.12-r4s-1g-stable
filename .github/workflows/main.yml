name: Auto Build and Release ImmortalWrt Stable for R4S 1G

on:
  workflow_dispatch:
    inputs:
      force_build:
        description: "强制编译（忽略更新检查，即使源码没有新提交也编译）"
        required: false
        default: "false"
        type: boolean
  schedule:
    # UTC 每天 18:00 检查一次（北京时间次日凌晨 2:00）；没有新提交时会自动跳过编译，不浪费 Actions 分钟数
    - cron: '0 18 * * *'

env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt
  # 锁定当前最新稳定分支（25.12），而非 master 开发分支，保证编译出的是稳定版
  # 升级到更新的稳定版时，只需把这里换成新分支名，例如未来的 openwrt-26.xx
  REPO_BRANCH: openwrt-25.12
  TZ: Asia/Shanghai

jobs:
  # ───────────────────────────────────────────────
  # 第一阶段：检查上游是否有新提交，没有就直接跳过编译，节省 Actions 用量
  # ───────────────────────────────────────────────
  check-update:
    runs-on: ubuntu-22.04
    outputs:
      should_build: ${{ steps.check.outputs.should_build }}
      remote_commit: ${{ steps.check.outputs.remote_commit }}
    steps:
      - name: Check upstream for new commits
        id: check
        run: |
          REMOTE_COMMIT=$(git ls-remote ${{ env.REPO_URL }} refs/heads/${{ env.REPO_BRANCH }} | cut -f1)
          echo "remote_commit=$REMOTE_COMMIT" >> "$GITHUB_OUTPUT"

          if [ "${{ github.event.inputs.force_build }}" = "true" ]; then
            echo "手动强制编译，跳过更新检查"
            echo "should_build=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # 用 GitHub API 查最新 release 的 tag 里记录的上游 commit，没有 release 记录就直接编译
          LATEST_TAG=$(curl -fsSL -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/latest" \
            | grep -o '"tag_name": *"[^"]*"' | sed 's/.*"\([^"]*\)"$/\1/' || true)

          if [ -z "$LATEST_TAG" ]; then
            echo "未找到历史 Release，首次编译"
            echo "should_build=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if echo "$LATEST_TAG" | grep -q "$REMOTE_COMMIT"; then
            echo "上游无新提交（commit: $REMOTE_COMMIT），跳过本次编译"
            echo "should_build=false" >> "$GITHUB_OUTPUT"
          else
            echo "检测到上游新提交：$REMOTE_COMMIT，开始编译"
            echo "should_build=true" >> "$GITHUB_OUTPUT"
          fi

  # ───────────────────────────────────────────────
  # 第二阶段：实际编译固件（仅当检测到更新或手动强制触发时运行）
  # ───────────────────────────────────────────────
  build:
    needs: check-update
    if: needs.check-update.outputs.should_build == 'true'
    runs-on: ubuntu-22.04
    permissions:
      contents: write
      actions: write

    steps:
      - name: Free up disk space
        run: |
          echo "清理前磁盘占用："
          df -h /
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo rm -rf /opt/hostedtoolcache
          sudo rm -rf /usr/local/share/boost /usr/local/graalvm* /usr/local/.ghcup
          sudo rm -rf /usr/lib/jvm /usr/share/swift /usr/local/julia*
          sudo docker image prune --all --force 2>/dev/null || true
          sudo apt-get clean
          echo "清理后磁盘占用："
          df -h /

      - name: Increase swap space
        run: |
          # dockerman/passwall/homeproxy 等插件编译期内存占用较高，加 swap 防 OOM
          AVAIL_GB=$(df --output=avail -BG / | tail -1 | tr -dc '0-9')
          SWAP_GB=$(( AVAIL_GB / 4 > 8 ? 8 : AVAIL_GB / 4 ))
          [ "$SWAP_GB" -lt 1 ] && SWAP_GB=1
          echo "分配 ${SWAP_GB}G swap"
          sudo swapoff -a
          sudo fallocate -l ${SWAP_GB}G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          free -h

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize compilation environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
            python3-distutils python3-pip python3-pyelftools python3-setuptools \
            swig rsync unzip zlib1g-dev file wget curl
          # apt 的 python3-pyelftools 才是 OpenWrt prereq.mk 实际检测的包；pip 仅作兜底
          sudo -E pip3 install --upgrade pyelftools
          python3 -c "import elftools; print('pyelftools OK:', elftools.__file__)"
          sudo timedatectl set-timezone "$TZ"

      - name: Clone ImmortalWrt source code
        run: git clone --depth 1 $REPO_URL -b $REPO_BRANCH openwrt

      - name: Record source commit
        working-directory: ./openwrt
        run: |
          echo "SOURCE_COMMIT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
          echo "SOURCE_COMMIT_FULL=$(git rev-parse HEAD)" >> $GITHUB_ENV

      - name: Get week number
        id: weeknum
        run: echo "week=$(date +%Y-%U)" >> $GITHUB_OUTPUT

      - name: Cache downloaded packages (dl)
        # GitHub 单仓库缓存总上限 10GB，超出会自动淘汰最旧缓存，属已知限制
        uses: actions/cache@v4
        with:
          path: openwrt/dl
          key: dl-packages-${{ env.REPO_BRANCH }}-${{ steps.weeknum.outputs.week }}
          restore-keys: |
            dl-packages-${{ env.REPO_BRANCH }}-

      - name: Update and install feeds
        working-directory: ./openwrt
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Generate custom configuration
        working-directory: ./openwrt
        run: |
          rm -f .config

          # ── 1. 基础目标：Rockchip RK3328 (NanoPi R4S) ──
          cat >> .config <<-EOF
          CONFIG_TARGET_rockchip=y
          CONFIG_TARGET_rockchip_armv8=y
          CONFIG_TARGET_rockchip_armv8_DEVICE_friendlyarm_nanopi-r4s=y
          EOF

          # ── 2. 分区大小：1G 设备的合理分配 ──
          # kernel 分区 32M 足够；rootfs 给到 1536M（约 1.5GB ext4 可用空间）
          # 原因：dockerman 本体 + docker 引擎 + passwall(含 v2ray/xray/sing-box 等核心) +
          # homeproxy(sing-box) 三者叠加，软件包本身就接近 700-900MB，
          # 再加上日志、临时容器层、未来 opkg 升级留白，1024M 比较紧张，1536M 更稳妥。
          # 本次新增 diskman/netdata/samba4 体积都不大（合计预估 <10MB），暂不调整分区，
          # 如果后续编译/运行后发现空间紧张，再考虑调到 2048。
          # 如果你的 TF/eMMC 容量较小（例如 8G 卡），可以调回 1024。
          cat >> .config <<-EOF
          CONFIG_TARGET_KERNEL_PARTSIZE=32
          CONFIG_TARGET_ROOTFS_PARTSIZE=1536
          EOF

          # ── 3. 预装指定插件 ──
          cat >> .config <<-EOF
          CONFIG_PACKAGE_luci-app-dockerman=y
          CONFIG_PACKAGE_luci-app-passwall=y
          CONFIG_PACKAGE_luci-app-homeproxy=y
          CONFIG_PACKAGE_luci-app-upnp=y
          EOF

          # ── 3.1 磁盘管理 diskman（管理内置 eMMC/SD 卡分区）──
          # 官方预编译仓库里以下4个内核模块在 rockchip/armv8 这个 target 不提供（kmod-fs-msdos
          # 和3个NLS codepage），apk/opkg 直接装会因依赖缺失报错，源码编译时显式选中即可正常编出。
          # 其余 diskman 标准依赖（blkid/parted/lsblk/dosfstools/e2fsprogs/exfat-fsck/exfat-mkfs/
          # smartmontools）官方仓库本身就有，feeds 依赖解析会自动带上，这里不用手写。
          cat >> .config <<-EOF
          CONFIG_PACKAGE_luci-app-diskman=y
          CONFIG_PACKAGE_luci-i18n-diskman-zh-cn=y
          CONFIG_PACKAGE_kmod-fs-msdos=y
          CONFIG_PACKAGE_kmod-nls-cp932=y
          CONFIG_PACKAGE_kmod-nls-cp936=y
          CONFIG_PACKAGE_kmod-nls-cp950=y
          EOF

          # ── 3.2 系统监控 netdata ──
          cat >> .config <<-EOF
          CONFIG_PACKAGE_luci-app-netdata=y
          CONFIG_PACKAGE_netdata=y
          CONFIG_PACKAGE_luci-i18n-netdata-zh-cn=y
          EOF

          # ── 3.3 NAS共享 samba4 ──
          # 注意：你选的是"diskman管理内置eMMC/SD卡分区"，samba4本身不挑存储介质，
          # 装好后照样可以把SD卡上的某个分区共享出去，二者互不冲突。
          cat >> .config <<-EOF
          CONFIG_PACKAGE_luci-app-samba4=y
          CONFIG_PACKAGE_luci-i18n-samba4-zh-cn=y
          EOF

          # ── 4. GC404 系优化：网络性能与转发效率 ──
          # 这些是 R2S/R4S 社区圈（天灵/GC/QC）长期沿用的一组标准优化项：
          # - Flow Offloading（软件层快速转发）+ Full Cone NAT：显著降低 NAT 后跑游戏/P2P 的延迟和连接问题
          # - BBR 拥塞控制：替换默认 cubic，高延迟/弱网链路下吞吐更稳定
          # - 关闭不必要的 IPv6 隧道安装脚本，减小固件体积（如需 IPv6 可自行在 LuCI 里启用 odhcp6c）
          cat >> .config <<-EOF
          CONFIG_PACKAGE_kmod-nf-nathelper=y
          CONFIG_PACKAGE_kmod-nf-nathelper-extra=y
          CONFIG_PACKAGE_luci-app-firewall=y
          CONFIG_PACKAGE_kmod-tcp-bbr=y
          CONFIG_PACKAGE_ethtool=y
          CONFIG_PACKAGE_irqbalance=y
          EOF

          # ── 5. 实用工具（GC404 类固件常见标配，体积很小但很常用）──
          cat >> .config <<-EOF
          CONFIG_PACKAGE_luci-app-ttyd=y
          CONFIG_PACKAGE_luci-app-watchdog=y
          CONFIG_PACKAGE_openssh-sftp-server=y
          CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-dockerman-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-passwall-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-homeproxy-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-package-manager-zh-cn=y
          EOF

          make defconfig

      - name: 核对本次新增插件是否选中成功
        working-directory: ./openwrt
        run: |
          echo "---- diskman 相关 ----"
          grep -E "luci-app-diskman|luci-i18n-diskman|kmod-fs-msdos|kmod-nls-cp9" .config || echo "(未匹配到，请检查上面 .config 写入是否出错)"
          echo "---- netdata 相关 ----"
          grep -E "luci-app-netdata|CONFIG_PACKAGE_netdata=y|luci-i18n-netdata" .config || echo "(未匹配到)"
          echo "---- samba4 相关 ----"
          grep -E "luci-app-samba4|luci-i18n-samba4" .config || echo "(未匹配到)"

      - name: Download packages
        working-directory: ./openwrt
        run: |
          for i in 1 2 3; do
            make download -j8 && break
            echo "第 $i 次下载未完全成功，清理可疑文件后重试..."
            find dl -size -1024c -exec rm -f {} \;
            sleep 10
          done
          make download -j1
          find dl -size -1024c | grep -q . && echo "::warning::仍有部分包体积异常，请检查日志" || echo "所有包下载完整"

      - name: Compile the firmware
        id: compile
        working-directory: ./openwrt
        run: |
          make -j$(nproc) || make -j1 || make -j1 V=s

      - name: Upload build logs (only on failure)
        if: failure() && steps.compile.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ github.run_id }}
          path: |
            openwrt/logs/
            openwrt/.config
          retention-days: 5

      - name: Check rootfs usage (warn if near full)
        working-directory: ./openwrt
        run: |
          # 编译完成后检查一下实际生成的 rootfs 占用比例，提前预警空间是否够用
          cd bin/targets/*/*
          IMG=$(ls *-squashfs-sysupgrade.img.gz 2>/dev/null | head -1 || true)
          if [ -n "$IMG" ]; then
            SIZE_MB=$(du -m "$IMG" | cut -f1)
            echo "固件大小：${SIZE_MB}M（rootfs 分区上限 1536M，压缩前的实际占用请刷机后在 LuCI 系统状态里看 df -h）"
            if [ "$SIZE_MB" -gt 1200 ]; then
              echo "::warning::固件压缩包已超过 1200M，加上后续 opkg/apk 升级和容器层占用，1536M 分区可能会比较紧张，建议关注"
            fi
          fi

      - name: Organize output files and generate Tag
        id: organize
        run: |
          cd openwrt/bin/targets/*/*
          rm -rf packages
          echo "FIRMWARE=$PWD" >> $GITHUB_ENV
          echo "RELEASE_TAG=$(date +"%Y.%m.%d-%H%M")-${SOURCE_COMMIT}" >> $GITHUB_ENV
          echo "RELEASE_DATE=$(date +"%Y年%m月%d日 %H:%M")" >> $GITHUB_ENV

      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: 🚀 ImmortalWrt R4S (1G) 稳定版自动构建 - ${{ env.RELEASE_DATE }}
          body: |
            ## 🔄 自动检测更新并编译
            - **设备**: NanoPi R4S (1G 内存版本)
            - **源码分支**: [ImmortalWrt ${{ env.REPO_BRANCH }}](${{ env.REPO_URL }}/tree/${{ env.REPO_BRANCH }})（稳定版分支，非 master 开发版）
            - **构建 Commit**: [`${{ env.SOURCE_COMMIT }}`](${{ env.REPO_URL }}/commit/${{ env.SOURCE_COMMIT_FULL }})
            - **RootFS 空间**: 1536 MB（维持不变）

            ## 📦 已集成插件
            - 🐳 `luci-app-dockerman`（含 Docker 引擎）
            - 🛡️ `luci-app-passwall`
            - 🚀 `luci-app-homeproxy`
            - 🔌 `luci-app-upnp`
            - 💾 `luci-app-diskman`（磁盘/分区管理，含官方仓库未提供的 kmod-fs-msdos、kmod-nls-cp932/936/950，源码编译解决依赖缺失问题）
            - 📊 `luci-app-netdata`（系统监控）
            - 🗂️ `luci-app-samba4`（NAS 文件共享）
            - 🖥️ `luci-app-ttyd`（网页终端）
            - 📟 `luci-app-watchdog`

            ## ⚙️ 网络优化（参考天灵/GC社区常见配置）
            - Flow Offloading + Full Cone NAT
            - BBR 拥塞控制算法
            - irqbalance 多核中断均衡
            - 中文语言包预装

            💡 **刷写说明**: 全新刷写下载 `squashfs-sysupgrade.img.gz`；在线升级请在 LuCI 的系统→备份升级中上传同一文件，并视情况选择是否保留配置。
          files: |
            ${{ env.FIRMWARE }}/*.img.gz
            ${{ env.FIRMWARE }}/*sha256sums*
            ${{ env.FIRMWARE }}/*.manifest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete older releases
        uses: dev-drprasad/delete-older-releases@v0.3.3
        with:
          keep_latest: 3
          delete_tags: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete older workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          retain_days: 14
          keep_minimum_runs: 5
